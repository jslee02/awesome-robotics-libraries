[workspace]
name = "awesome-robotics-libraries"
channels = ["conda-forge"]
platforms = ["linux-64", "osx-arm64", "osx-64", "win-64"]

[dependencies]
python = ">=3.10"
pyyaml = "*"
jsonschema = "*"

[tasks]
validate = "python3 scripts/validate_entries.py"
generate = "python3 scripts/generate_readme.py"
check = { depends-on = ["validate", "generate"] }
